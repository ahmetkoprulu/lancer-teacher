import { Student } from './student';

export class Rate {
    id: number;
    score: number;
    comment: string;

    // tslint:disable-next-line:variable-name
    c_id: number;
    // tslint:disable-next-line:variable-name
    s_id: number;
    student: Student;
}
